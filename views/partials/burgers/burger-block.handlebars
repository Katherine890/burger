<li>
	{{burger_name}}

	<!--<button class="devour-it" data-id="{{id}}" data-devourIt="{{devoured}}">
		Devour It!
	</button>-->

	{{#if devoured}}Devoured{{else}}<button class="devour-it" data-id="{{id}}" data-devourIt="{{devoured}}">
		Devour It!</button>{{/if}}

</li>